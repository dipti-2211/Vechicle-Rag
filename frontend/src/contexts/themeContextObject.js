import { createContext } from 'react';

/**
 * The React context object for theme state.
 * Import this in ThemeContext.jsx (provider) and useTheme.js (hook).
 */
export const ThemeContext = createContext();
